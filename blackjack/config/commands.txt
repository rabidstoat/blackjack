# This file will hold the game commands that are
# implemented by the blackjack server. They are
# loaded via reflection. Need to specify fully
# qualified (with path) classes that implement the
# BlackjackCommand interface.